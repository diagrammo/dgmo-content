{
  "name": "@diagrammo/content",
  "version": "0.0.0",
  "private": true,
  "description": "Shared DGMO examples, chart-type guides and the canonical registry. Never published — consumed by symlink and by git submodule.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'",
    "check": "node scripts/check-all.mjs",
    "prepare": "git config core.hooksPath .githooks"
  },
  "engines": {
    "node": ">=20"
  }
}
